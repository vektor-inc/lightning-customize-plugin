# grunt-wp-readme-to-markdown が要求する grunt 0.4 系と、実際に使う grunt 1.6 系の peer 依存が
# 競合するため、npm 7 以降の厳密な peer 依存チェックを無効にする。無効にしないと npm install / npm ci が
# ERESOLVE で失敗する。Dependabot もこの設定で lock を生成する。
legacy-peer-deps=true
